# Main libraries
pyspark==3.3.0
spacy==3.4.4
transformers==4.24.0
torch==1.13.1
numpy==1.21.6

# SpaCy model (needs to be downloaded separately)
# Run this command after installation to get the model:
# python -m spacy download en_core_web_sm
